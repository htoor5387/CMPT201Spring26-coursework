#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {

  // pid_t pid=fork() to get PID
  int pid = fork();
  if (pid != 0) {
    char *args[] = {"ls -a", NULL};
    execvp(args[0], args);
  } // parent
  else {
    char *args[] = {"ls -a", NULL};
    execvp(args[0], args);
  } // child

  printf("DONE");
}
