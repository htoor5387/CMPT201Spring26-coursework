#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {

  // pid_t pid=fork() to get PID
  int pid = fork();
  if (pid != 0) {
    execlp("/home", "ls", "-a", NULL);
  } // parent
  else {
    execlp("/home", "ls", "-alh", NULL);
  } // child

  printf("DONE");
}
