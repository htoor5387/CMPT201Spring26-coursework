#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main() {

  // pid_t pid=fork() to get PID
  int pid = fork();
  if(pid!=0){
    char *args[]={"ls -a"};
    execvp(args[0], args);
  }//parent
  else{
    //execcvp
  }//child

  printf("DONE");
}
